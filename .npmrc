# Independent package - don't inherit parent monorepo config
# This ensures AIGILE works as a standalone npm package

# Use npm's default registry
registry=https://registry.npmjs.org/

# Don't hoist to parent node_modules
install-strategy=nested
